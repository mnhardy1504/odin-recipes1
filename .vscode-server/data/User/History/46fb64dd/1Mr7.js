const multiply = function(a, b) {    // function to multiply tw numbers
    return a * b; 
}

console.log(multiply(2,2))

const divide = function(a, b) {
   if (b === 0) {
    return "Cannot divide by zero";
   } else {
     return a / b; 
   }
};
